export const PLAYBACK_DATA_ROUTE = "/settings/playback-sessions";
/** Raw reads retained in the frequently refreshed activity sample. */
export const PLAYBACK_HISTORY_LIMIT = 500;
/** Grouped plays returned from the lazily loaded retained Playback history. */
export const DEEP_PLAYBACK_HISTORY_LIMIT = 200;
